{
  "name": "arbitex",
  "version": "0.0.1",
  "private": true,
  "description": "Arbitex — AI-powered trading infrastructure for Bitget",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
